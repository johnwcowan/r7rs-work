This proposal has been flushed.  The only things it added were `reverse-assoc` and `make-alist`, which are trivially implemented using `find` and `zip` (more or less; `zip` won't make dotted pairs).
